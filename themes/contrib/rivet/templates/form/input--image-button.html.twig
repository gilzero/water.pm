{#
/**
 * @file
 * Theme override for an image button submit form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 *
 * @see template_preprocess_input()
 */
#}
<input{{ attributes.removeClass('image-button').addClass('rvt-c-image-button') }}/>
