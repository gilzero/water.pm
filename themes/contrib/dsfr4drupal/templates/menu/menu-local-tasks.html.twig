{% if primary %}
  <h2 class="visually-hidden">{{ 'Primary tabs'|t }}</h2>
  {{ include('dsfr4drupal:tabs', {'tabs': primary}) }}
{% endif %}
{% if secondary %}
  <h2 class="visually-hidden">{{ 'Secondary tabs'|t }}</h2>
  {{ include('dsfr4drupal:tabs', {'tabs': secondary}) }}
{% endif %}
